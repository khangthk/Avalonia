<Style xmlns="https://github.com/avaloniaui"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Selector="TabStrip">
  <Design.PreviewWith>
    <Border Padding="20">
      <TabStrip>
        <TabStripItem>Item 1</TabStripItem>
        <TabStripItem>Item 2</TabStripItem>
        <TabStripItem IsEnabled="False">Disabled</TabStripItem>
      </TabStrip>
    </Border>
  </Design.PreviewWith>
  <Style.Resources>
    <ControlTheme x:Key="FluentTabStrip" TargetType="TabStrip">
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}" 
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  Padding="{TemplateBinding Padding}">
            <ItemsPresenter Name="PART_ItemsPresenter" 
                            Items="{TemplateBinding Items}" 
                            ItemsPanel="{TemplateBinding ItemsPanel}"
                            ItemTemplate="{TemplateBinding ItemTemplate}" />
          </Border>
        </ControlTemplate>
      </Setter>
      <Setter Property="ItemsPanel">
        <ItemsPanelTemplate>
          <WrapPanel />
        </ItemsPanelTemplate>
      </Setter>
    </ControlTheme>
  </Style.Resources>

  <Setter Property="Theme" Value="{StaticResource FluentTabStrip}" />
</Style>
